# sif-pic-website

This repository now contains a minimal Vite + React scaffold.

Getting started

1. Install dependencies:

	npm install

2. Start the dev server:

	npm run dev

Open http://localhost:5173 in your browser.

